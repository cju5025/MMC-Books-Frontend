import { Component } from 'react';

import Header from './header';

export default class Merch extends Component {
    render () {
        return (
            <main id="merch-main">
                <Header />
                <h1>Merch In/Out</h1>
            </main>
        )
    }
}