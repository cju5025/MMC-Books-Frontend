import { Component } from 'react';

import Header from './header';

export default class Misc extends Component {
    render () {
        return (
            <main id="misc-main">
                <Header />
                <h1>Misc In/Out</h1>
            </main>
        )
    }
}